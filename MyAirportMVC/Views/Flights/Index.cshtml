@model IEnumerable<MyAirportMVC.Models.Flight>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New flight", "Create")
    <button type="button" onclick="location.href='@Url.Action("Index", "FlightInfoes")'">New way</button>
</p>



@*@using (Html.BeginForm("Index", "Flight", FormMethod.Get))
{
    @Html.TextBox("searching")<input type="submit" value="Search" />
}


<table class=" table table-striped">

    <thead>

        <tr>FlightNumber</tr>
        <tr>Destination</tr>
        <tr>Departure_DateTime</tr>
        <tr>Arrival_DateTime</tr>
        <tr>Airline</tr>
        <tr>Terminal</tr>
        <tr>ClassSeats</tr>
        <tr>Gate</tr>
        <tr>Status</tr>
    </thead>

    <tbody>
        @if (Model.Count() == 0)
        {
            <tr>
                <td colspan="9" style=" color: red">

                    No math
                </td>

            </tr>
        }
        else
        {


            foreach (var item in Model)
            {
                <tr>
                    <td> @item.FlightNumber </td>
                    <td> @item.Destination </td>
                    <td> @item.Departure_DateTime </td>
                    <td> @item.Arrival_DateTime </td>
                    <td>@item.Airline </td>
                    <td> @item.Terminal </td>
                    <td> @item.Gate </td>
                    <td> @item.Status </td>


                </tr>
            }
        }


    </tbody>

</table>*@


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FlightNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Destination)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Departure_DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Arrival_DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Airline)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Terminal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ClassSeats)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FlightNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Destination)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departure_DateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Arrival_DateTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Airline)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Terminal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClassSeats)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Flight_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.Flight_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Flight_ID })

                <button type="button" onclick="location.href='@Url.Action("Getinfo", "FlightInfoes", new { id = item.Flight_ID } )'">Buy</button>

            </td>
        </tr>
    }

</table>
